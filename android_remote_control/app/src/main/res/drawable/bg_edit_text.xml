<?xml version="1.0" encoding="utf-8"?>
<!--
    Rounded rectangle background for the EditText fields in the connection bar.
    Using a shape drawable instead of a flat colour guarantees an intrinsic
    minimum size so the field never collapses to zero height on any device.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#3C3C3C" />
    <corners android:radius="6dp" />
    <stroke
        android:width="1dp"
        android:color="#555555" />
</shape>
